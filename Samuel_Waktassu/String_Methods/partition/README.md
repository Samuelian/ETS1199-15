# str.partition() in Python

## What It Does
- Splits a string into **three parts** based on the first occurrence of a **separator**.
- Returns a tuple: `(before_separator, separator, after_separator)`.

## Syntax
```python
str.partition(sep)
